{
  "name": "mart-mail-app",
  "version": "1.0.0",
  "description": "MART Mail Generator Android App",
  "scripts": {
    "build": "echo 'No build needed for web app'",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
